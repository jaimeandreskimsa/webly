# Usar resolución legacy de peer deps para evitar warnings de conflictos entre
# @auth/core@0.41.0 (next-auth) y @auth/core@0.41.1 (@auth/drizzle-adapter)
legacy-peer-deps=true
prefer-offline=false
# Equivalente moderno de --production para que Railway no muestre el aviso
omit=dev
